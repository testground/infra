apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: observability
spec:
  generators:
  - git:
      repoURL: https://github.com/celestiaorg/testground-infra.git
      revision: jose/hackground-k8s-tf
      directories:
      - path: 'kustomize/observability/*'
      - path: kustomize/observability/deactivated
        exclude: true
  template:
    metadata:
      name: 'observability-{{path[1]}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/celestiaorg/testground-infra.git
        path: 'kustomize/observability/{{path[1]}}'
      destination:
        name: in-cluster
        namespace: default
      syncPolicy:
        automated:
          prune: true
          allowEmpty: true
          selfHeal: true
        syncOptions:
        - ApplyOutOfSyncOnly=true
        - CreateNamespace=true
