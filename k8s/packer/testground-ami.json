  {
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "eu-west-2",
    "source_ami": "ami-05516775460f4eb85",
    "instance_type": "m4.xlarge",
    "ssh_username": "admin",
    "ami_name": "testground_ami_{{isotime \"2006-01-02\"}}_{{timestamp}}"
  }],
  "provisioners": [
  {
    "type": "shell",
    "script": "docker-pull-images.sh"
  }
  ]
}
