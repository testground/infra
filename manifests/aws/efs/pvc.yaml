kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: efs
    #annotations:
    #  volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: aws-efs
  resources:
    requests:
      storage: 1000Mi
